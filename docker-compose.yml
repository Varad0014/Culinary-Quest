version: "3.9"

services:
  mongo_db:
    container_name: mongo_container
    image: jaguar/restaurantmongo:latest

  backend:
    image: jaguar/backend-img
    ports:
      - "4000:4000"
    environment:
      - DBURL=mongodb://mongo_db:27017/restaurantsDB
      - PORT=4000
  frontend:
    image: jaguar/frontend-img
    ports:
      - "3000:3000"
